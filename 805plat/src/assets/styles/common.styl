@require "./var.styl"
*{
    box-sizing: border-box;
}

html,body{
    font-size:30px;
    background $color-bg;
    height: 100%;
}

.tab-title{
    font-size:$size-m;
    margin-bottom $padding-m
}

